#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (successs)
 */

void print_alphabet_x10(void);
{
	int a;

	char b;

	for (a = 0; i < 10; ++a)
	{
		for (b = 'a'; c <= 'z'; ++b)
		{
			_putchar(a);
		}
		_putchar('\n');
	}

	return (0);
}
